新增 Factorio 2.0 → 2.1 数据适配层（adapt 模块）

schema（prototype-api.json）面向 2.1.14；2.0 导出的 data-raw-dump.json
字段形态有差异，直接反序列化会失败。从官方 schema 更新 commit 86317bd
推导字段变化，提供就地规范化适配层，每个适配层一个独立函数便于扩展。

首个适配层：adapt_recipe_categories —— 2.0 recipe 的 category（标量）
+ additional_categories（数组）在 2.1 合并为单个 categories 数组
（category 在前、additional 依次追加、去重；已含 categories 的 2.1
形态幂等不变）。

PrototypeStore::load 在反序列化前调用 normalize_2_0_dump；含
category 合并端到端加载测试与 7 个单测。
