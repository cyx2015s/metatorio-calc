适配层优化：needs_adaptation 只读检测避免 2.1 dump 深拷贝

PrototypeStore::load 之前无条件克隆整个 dump 再规范化——29MB 的
2.1 dump 每次加载都白做一次深拷贝。新增 adapt::needs_adaptation
（只读遍历 recipe 组，命中 category/additional_categories 任一 2.0
特征才返回 true），load 仅在需要时克隆改写；2.1 dump 直接复用原
Value。补检测单测。
