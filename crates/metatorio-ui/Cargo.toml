[package]
name = "metatorio-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.35" }
egui = { version = "0.35", features = ["rayon"] }
metatorio-core = { path = "../metatorio-core" }

[features]
default = ["wgpu"]
wgpu = ["eframe/wgpu"]
